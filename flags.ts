// Global test flags for notification debugging
export const TEST_SCHEDULER_ONLY = false;  // false to enable reminder/text notifications
export const NUDGES_ENABLED = true;        // enable nudges/auto paths for random notifications